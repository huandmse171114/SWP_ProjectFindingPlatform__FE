import styles from './MemberProfile.module.scss'
import classNames from 'classnames/bind';

const cx = classNames.bind(styles);

function MemberProfile() {
    return (
        <h1>Member Profile Component</h1>
    );
}

export default MemberProfile;